--[[
	╔══════════════════════════════════════════════════════════════╗
	║  Config  —  ТИП: ModuleScript                                  ║
	║  ПОЛОЖИТЬ В:  ReplicatedStorage > Shared > Config              ║
	╚══════════════════════════════════════════════════════════════╝

	Единственное место для настройки баланса. И сервер, и клиент
	читают эти значения, поэтому всё всегда совпадает.
]]

local Config = {}

-- ВАЛЮТЫ -------------------------------------------------------------------
Config.Currency = {
	OofName  = "Oof",   -- основная валюта (её приносит ноб)
	GemsName = "Gems",  -- премиум-валюта (капает по таймеру)
}

-- НАЧАЛЬНЫЕ ЗНАЧЕНИЯ НОВОГО ИГРОКА -----------------------------------------
Config.Start = {
	Oof  = 0,
	Gems = 0,
}

-- DATASTORE ----------------------------------------------------------------
Config.Data = {
	StoreName = "OofIncremental_v1", -- смени _v1 -> _v2 чтобы обнулить ВСЕ сейвы
	AutoSaveInterval = 60,           -- автосейв каждые N секунд
}

-- NPC ("Noob") -------------------------------------------------------------
Config.Noob = {
	BuyCost = 0,        -- цена первого ноба (0 = бесплатно по кнопке)

	BaseInterval = 3,   -- раз в сколько секунд ноб приносит Oof
	BaseReward   = 1,   -- доход за тик на 1 уровне

	MaxLevel          = 1000,
	UpgradeBaseCost   = 10,    -- цена прокачки 1 -> 2 уровень
	UpgradeCostGrowth = 1.35,  -- во сколько раз дорожает каждый уровень
	RewardPerLevel    = 1,     -- +доход за каждый уровень

	RigName     = "NoobRig",                 -- имя модели-рига в ServerStorage
	AnimationId = "rbxassetid://507770677",  -- анимация ноба (по умолч. "Cheer")
	DisplayName = "Starter Noob",            -- имя над нобом
}

-- УЛУЧШЕНИЯ НА ДОСКЕ (SurfaceGui). Порядок = порядок кнопок слева направо --
Config.Upgrades = {
	{
		Id = "MoreOof",
		DisplayName = "More Oof",
		Desc = "[x2 every 15 upgrades]",
		MaxLevel = 50,
		BaseCost = 75,
		CostGrowth = 1.6,
	},
	{
		Id = "FasterNoobs",
		DisplayName = "Faster Noobs",
		Desc = "Speeds up your noob",
		MaxLevel = 5,
		BaseCost = 10000,
		CostGrowth = 4,
		IntervalReductionPerLevel = 0.1, -- −0.1с к интервалу за уровень
	},
}

-- ГЕМЫ: пассивный доход по таймеру (как на скрине: +2.24 [35s]) ------------
Config.Gems = {
	Interval = 35, -- раз в сколько секунд капают гемы
	Amount   = 2,  -- сколько гемов за тик
}

-- ПРЕСТИЖ (только для шкалы прогресса вверху) ------------------------------
Config.Prestige = {
	BaseRequirement = 5e12, -- 5T Oof для Prestige 1 (как на скрине "/5T")
	Growth = 10,            -- во сколько раз дорожает каждый следующий престиж
}

-- ПЛОТЫ (личные участки игроков) ------------------------------------------
Config.Plot = {
	Origin  = Vector3.new(0, 5, 0), -- центр первого плота
	Spacing = 100,                  -- расстояние между плотами по X
}

-- ТУТОРИАЛ-СТРЕЛКА К КНОПКЕ ------------------------------------------------
-- Сюда вставишь ID своей загруженной картинки со стрелкой.
Config.ArrowImageId = "rbxassetid://0"

return Config
